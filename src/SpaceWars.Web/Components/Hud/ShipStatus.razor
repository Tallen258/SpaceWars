@using SpaceWars.Logic

@if (Ship == null)
{
    <p>Ship not found</p>
    return;
}
else
{
    <div class="container">
        <div class="row border border-3 rounded p-2 w-100">
            <div class="col-6">
                <ProgressBar Label="Shield" Value="@Ship.Shield" />
                <ProgressBar Label="Health" Value="@Ship.Health" />
                <p> Ship Location: (@Ship.Location.X, @Ship.Location.Y)</p>
            </div>
            <div class="col-6">
                <label for="speed">Speed</label>
                <h6>@Ship.Speed</h6>
                <label for="heading">Heading</label>
                <h6>@Ship.Heading&deg;</h6>
                <Compass Heading="@Ship.Heading" />
            </div>
        </div>
    </div>
}

@code {
    [Parameter] public Ship Ship { get; set; }

}
